<%= form_for([@post, @post.comments.build]) do |f| %>
<p>
    <%= f.label :name %><br/>
    <%= f.text_field :name, :value => @post.title %>
</p>

<p>
	<%= f.label :body %><br/>
	<%= f.text_area :body %>
</p>

<p>
	<%= f.submit %>
</p>

<% end %>
